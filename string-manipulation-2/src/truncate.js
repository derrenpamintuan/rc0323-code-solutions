/* exported truncate */
function truncate(length, string) {
  return string.slice(0, length) + '...';
}

/** Declare a function named 'truncate', that accepts two arguments, 'length' and 'string'
 *    Return the slice method of the string object at index 0 to 'length' concatenated with '...'
 */
