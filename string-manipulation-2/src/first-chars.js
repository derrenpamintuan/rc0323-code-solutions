/* exported firstChars */
function firstChars(length, string) {
  return string.slice(0, length);
}

/** Define a function named 'firstChars', that accepts two arguments, 'length' and 'string'
 *    return the value of string from index 0 to length
 */
