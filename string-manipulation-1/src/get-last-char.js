/* exported getLastChar */
function getLastChar(string) {
  return string[string.length - 1];
}

/** Define a function named getLastChar, that accepts one argument, 'string'
 *    return the value of the string object at the length method of the string object minus 1
 */
