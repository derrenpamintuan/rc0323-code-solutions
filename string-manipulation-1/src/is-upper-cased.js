/* exported isUpperCased */
function isUpperCased(word) {
  if (word === word.toUpperCase()) {
    return true;
  } else {
    return false;
  }
}

/** define a function named 'isUpperCased', that accepts one argument, 'word'
 *    if the condition of word object is strictly equal to the toUpperCase method of the word object
 *      return the value of true
 *    otherwise
 *      return the value of false
 */
