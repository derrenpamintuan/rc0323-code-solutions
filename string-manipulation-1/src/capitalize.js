/* exported capitalize */
function capitalize(word) {
  return word[0].toUpperCase() + word.slice(1).toLowerCase();
}

/** Define a function named 'capitalize', that accepts one argument, 'word'
 *    return the expression of toUpperCase method of the word object at index of 0 concatenated with toLowerCase method of the slice method at index 1 of the word object
*/
