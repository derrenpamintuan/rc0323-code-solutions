/* exported setValue */
function setValue(object, key, value) {
  object[key] = value;
}

/** Define a function named 'setValue', that accepts three arguments, 'object', 'key', and 'value'
 *    assign value to be object at key
 */
