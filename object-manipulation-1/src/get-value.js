/* exported getValue */
function getValue(object, key) {
  return object[key];
}

/** Define a function named 'getValues', that accepts two arguments, 'object' and 'key'
 *    return the value of object at key
 */
