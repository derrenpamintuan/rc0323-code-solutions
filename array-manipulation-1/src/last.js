/* exported last */
function last(array) {
  return array[array.length - 1];
}

/** Define a function named 'last', that accepts one argument, 'array'
 *    return the value of the array object at the length method of the array object minus 1
 */
